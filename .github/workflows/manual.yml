name: Manual workflow

on:
  workflow_dispatch:

jobs:
  dependencies:
    uses: MarcoEidinger/swift-package-dependencies-check/.github/workflows/reusableWorkflow.yml@2.3.2
    with:
      commit-message: 'chore: update package dependencies'
